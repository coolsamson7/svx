{
  "name": "shell",
  "root": "apps/shell",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vite",
        "cwd": "apps/shell"
      }
    },
    "generate-manifest": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx tsx --tsconfig tools/manifest-generator/tsconfig.json tools/manifest-generator/src/generate.ts {projectRoot} {projectRoot}/src/manifest.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
