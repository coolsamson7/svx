{
  "name": "@svx/di",
  "sourceRoot": "libs/di/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/dist/libs/di"],
      "options": {
        "command": "npx vite build --config vite.lib.config.ts",
        "cwd": "libs/di"
      }
    }
  }
}
