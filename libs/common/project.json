{
  "name": "@svx/common",
  "sourceRoot": "libs/common/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{workspaceRoot}/dist/libs/common"],
      "options": {
        "command": "npx vite build --config vite.lib.config.ts",
        "cwd": "libs/common"
      }
    }
  }
}
