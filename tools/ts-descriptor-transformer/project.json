{
  "name": "ts-descriptor-transformer",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc",
        "cwd": "tools/ts-descriptor-transformer"
      }
    }
  }
}
